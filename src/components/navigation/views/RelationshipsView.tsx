import type { Player } from '../../../types';
import { buildBondProfiles, scoreToStage, STAGE_LABELS } from '../../../types/relationship';
import { Users, Sparkles, Heart } from 'lucide-react';
export function RelationshipsView({ player }: { player: Player; onClose: () => void }) {
  return <div className="menu-view-container glass-panel animate-scale-up"><div className="menu-view-header"><div className="menu-view-title-group"><Users size={22}/><div><h3 className="menu-view-title">선수 인연 & 인간관계도</h3><p className="menu-view-sub">학년과 관계 단계에 따라 새로운 조언·훈련·연애 이벤트가 열립니다.</p></div></div></div><div className="menu-view-body"><div className="relationships-grid">{buildBondProfiles(player).map(p=>{const stage=scoreToStage(p.score); const romance=!!p.romanceable&&stage>=4; return <div className="rel-card glass-panel" key={p.id}><div className="rel-card-top"><span className="rel-icon">{p.icon}</span><div className="rel-info"><strong className="rel-name">{p.name}</strong><span className="rel-role">{p.role}</span></div><span className="rel-score-badge">{p.score} pt</span></div><div className="rel-gauge-track"><div className="rel-gauge-fill" style={{width:`${p.score}%`}}/></div><div className="rel-status-text">관계 {stage}단계 · <strong>{STAGE_LABELS[stage]}</strong></div><p className="rel-effect-desc"><Sparkles size={12}/> {p.note}</p>{p.romanceable&&<p className="rel-effect-desc"><Heart size={12}/> {romance?'데이트·고백 이벤트 활성화':'4단계부터 연애 이벤트 가능'}</p>}</div>})}</div></div></div>;
}
